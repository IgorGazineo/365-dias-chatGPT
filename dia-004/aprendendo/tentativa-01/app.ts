const numeros: number[] = [1, 2, 3, 4, 5];
const primeiroNumero: number = numeros[0];
const ultimoNumero: number = numeros[numeros.length - 1];

console.log(primeiroNumero);
console.log(ultimoNumero);
